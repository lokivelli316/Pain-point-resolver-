# Changelog

All notable changes are recorded here. Entries are added, never rewritten.
Format: [Keep a Changelog](https://keepachangelog.com/en/1.1.0/). Versioning: [SemVer](https://semver.org/).

## [Unreleased]

### Added
- Community system: pain point intake form, fix-idea form, lifecycle labels, label sync and welcome/`/hunt` workflows.
- README call to action, AI collaborator guidelines, launch posts.
- Open-shop floor: README / GOVERNANCE / AI collaborator appends; CODEOWNERS catch-all removed so branches do not wait on the founder. `main` stays the protected call.

## [0.1.0] — 2026-09-17

### Added
- Core: accessible reporter, hash-chained append-only register, environment fingerprint,
  ELF arch/libc inspector, static pre-flight scanner, calm command runner, pattern engine.
- Commands: `doctor`, `docs`, `hunt`, `calm`, `a11y`, `register`, `modules`, `pain`.
- Planned command stubs with specs: `up`, `install-api`, `deps`, `trace`.
- 27 known failure patterns with test samples.
- 62-point pain map as data (`data/pain_points.json`).

### Origin
- Rebuilt from ForgeDoc 1.0.0 (Termux shell script).
